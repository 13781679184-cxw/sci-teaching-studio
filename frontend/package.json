{
  "name": "sci-teaching-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:demo": "vite build --mode demo && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\"",
    "build:demo:github": "vite build --mode demo --base /sci-teaching-studio/ && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\"",
    "deploy:pages": "npm run build:demo && wrangler pages deploy dist --project-name=sci-teaching-studio",
    "preview": "vite preview",
    "preview:demo": "vite preview --mode demo"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0",
    "wrangler": "^4.111.0"
  }
}
